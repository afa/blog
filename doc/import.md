# импорт/экспорт

## модули импорта

* скачивание/загрузка, с постраничностью и контролём неизменности данных (плюс склейка страниц), должен уметь в целостность скачанного
* разбор скачанного на объекты, дотаскивание не хватающего из внешних справочников, предварительная чистка
* парсер полученных объектов, определение неизмененных данных, применение изменений
* конфигурация

## модули конфигурации

* источник данных, описание паджинации источника (ключи, номер начальной страницы, номер страницы по умолчанию без паджинации)
* 
*

## поведение

отдельная таблица олписания сущностей (ссыль на источник, имя, список аттрибутов (+ тип?), список ассоциаций, список 
ембедов ака объектов 1ту1),
 таблица хранения аттрибутов (ссылка на сессию импорта, ссылка на описание сущности, название поля, значение),
сессии устаревшие помечаются архивными, сохраняются только изменения аттрибутов с более новой версией.
парсятся до аттрибутов абсолютно все полученные данные, не описанные в сущностях аттрибуты помечаются сырыми.


репозитории - кеши редис и инстанс
список источников - как и импорт веб и джейсон, с паджинацией и без

## ?? деление помодульно

протокол -- как мы скачиваем, веб, json апи, параметризованно (постранично ака chunked), плюс возможные доппараметры
 определяемые в сетапе (из предыдущих сессий или вычисляемые)

стратегия -- как мы находим изменения, будет ли это инкрементальный скан всего сравнивая со снапшотом или всё 
полученное с зависящими от предыдущей загрузки параметрами является новым или необходимы предварительные запросы для получения
новой порции - задается в стратегии и уже дальше в соответствии с протоколом выгребаем необходимое

### поведение

синк зовёт выбранную стратегию, в соответствии с параметрами источника, стратегия определяет параметры запроса и зовёт
нужный протокол опять из параметров источника, который настраивается по параметрам источника и предыдущей сессии
